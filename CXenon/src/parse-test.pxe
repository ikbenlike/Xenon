use "stdio.h"

float:fib() {
    if (n == 0) { return 0; }
    if (n == 1) { return 1; }
    return fib(n - 1) + fib(n - 2);
}

int:main() {
    bool no = false;
    int n = 11;
    int i = 1.1;

    str stuff = "stuff"[1];
    int n = fib(stuff[1])[1];
    stuff[1];
    for(a in b){print(stuff);}
    while (stuff[1] == stuff[1]) {
        n = fib(10);
        stuff();
        print(n);
        i = i + 1;
    }
    loop {
        print(n);
    }
    return 0;
}
