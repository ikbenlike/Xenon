use "stdio.h"

int fib() {
    if (n == 0) { return 0; }
    if (n == 1) { return 1; }
    return fib(n - 1) + fib(n - 2);
}

int main() {

    int n = 11;
    int i = 11;

    str stuff = "stuff";

    while (i < 10) {
        n = fib(10);
        stuff();
        print(n);
        i = i + 1;
    }
    return 0;
}
